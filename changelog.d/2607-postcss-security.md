- **The website build toolchain includes the completed PostCSS source-map
  security fix (#2605).** The patched dependency closes an upstream file-read
  vulnerability in source-map handling. No configuration or operator action is
  required.
