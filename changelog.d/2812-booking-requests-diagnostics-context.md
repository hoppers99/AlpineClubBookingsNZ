- **AI Diagnostics now knows about the booking requests page (#2812).** Asking from
  the approvals, change requests, exceptions or public queue tabs carries that page's
  context to the assistant — previously its registry entry pointed at an address that
  only redirects, so the busiest "why will this booking not confirm?" screen had no
  context at all. The approvals queue also gains the same per-row stethoscope as the
  bookings, waitlist and payments lists, so a specific booking under review can be
  made the subject of a question directly.
