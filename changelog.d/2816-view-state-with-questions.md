- **Diagnostics questions now carry what you are looking at (#2816).** Asking from
  the bookings, waitlist, payments or members lists sends the page's current
  filters — the status you filtered to, the date window, the search — so "why am I
  not seeing this payment?" can be answered with your actual view in hand (a
  too-narrow activity window is the most common reason). Only what each page's
  registered allowlist permits is used; anything else in the address is dropped
  before it reaches the assistant.
