- The default hero subtitle on the Join, Apply and Contact pages is no longer
  rendered as HTML. Those pages compose that sentence themselves, and two of
  them build it from the club name — a free-text setting no sanitiser checks —
  so it is now displayed as plain text. A subtitle a club has written in Site
  Appearance & Content is unchanged: it is still rendered with its formatting.
  Nothing an existing site displays changes. (#2819)
