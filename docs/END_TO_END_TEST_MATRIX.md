# End To End Test Matrix

This first-pass matrix links personas, journeys, risks, test types, and likely
validation commands. Use it to plan focused issues and avoid claiming coverage
that does not exist yet.

Public CMS authoritative-block coverage must exercise enabled/disabled states,
home/code-backed/catch-all/404 parity, global and lodge variants, current-date
fee boundaries, divergent card/credit cancellation terms, invalid lodges,
sanitisation, and authority-write cache invalidation. Signed-out output must
contain no ids, provider codes, internal settings, or legacy entrance fallback.

The rows marked "Playwright E2E" below now have automated browser coverage in
`e2e/` run against the staging compose stack — see
[`E2E_PLAYWRIGHT.md`](E2E_PLAYWRIGHT.md). This spans the Critical booking,
two-factor, Stripe, and cancellation-with-refund rows plus the High
role-boundary, role-management, waitlist, internet-banking, bed-allocation,
partner-shared double-bed, and membership-application rows. The Stripe payment
specs skip unless genuine Stripe
test-mode keys are configured, and they cover **only test-mode payment success
and decline**. The cancellation-with-refund spec
(`e2e/booking-cancel-refund.spec.ts`) covers the cancel → **account-credit**
money outcome; **card-refund** (a real Stripe refund), saved-card, and
member-credit paths still have no browser coverage (Vitest/service only). The
deliberate, ratified browser-coverage gaps (cron-driven waitlist offer/expiry,
webhook signature classes) are listed in `E2E_PLAYWRIGHT.md`. Email-code 2FA is
covered by `e2e/two-factor-email.spec.ts`.

Concurrency-under-load coverage (the "stable at 100+ concurrent users"
target) lives outside this matrix in the k6 harness under `load/` — public
browse, login, member-dashboard reads, and the booking-hold contention
stampede over the per-lodge advisory lock. See
[`LOAD_TESTING.md`](LOAD_TESTING.md); runs are owner-gated and target only
the throwaway staging stack.

| Area                       | Persona or actor                     | Journey or behavior                                                                                                                                                                                                                                                                                                                                                                              | Risk     | Test type                                                                                                           | Suggested validation                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| -------------------------- | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Auth/access control        | Anonymous                            | Public pages, login, token routes, public API exceptions                                                                                                                                                                                                                                                                                                                                         | High     | Route/static tests plus manual public smoke                                                                         | Locate and run targeted route/security suites                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Privacy/disclosure         | Member/admin/public email recipient | Concrete-booking email deep links (#2362): only an active login-capable owner, linked member, or bookings-view admin receives the canonical encoded `/bookings/<id>` URL; deleted bookings stay Full-Admin-only. Public/non-login and aggregate recipients receive no authenticated detail URL. Retry repeats current authority from durable identity, strips a revoked built-in CTA, and retires unknown legacy context or a now-unauthorized immutable override. Bearer payment/quote/consent/respond actions and booking-page fragments remain intact; existing stored body overrides are not rewritten | High | Authority, HTML, retry, migration, registry/default, mutation-contract, wrapper, and suppression unit tests | Targeted `booking-email-authority`, `booking-email-html`, `cron-email-retry`, `email-message-token-contract`, `email-message-registry`, `booking-no-emails-gate`, `email-door-code-subject`, and member-guest real-core email suites; migration safety/duplicate-prefix/drift checks; mutation fixtures remove one inventory classification, retry reauthorization, and fragment preservation in turn and must report the break; no live SES or browser automation |
| Auth/access control        | Member/admin/finance/lodge           | Global two-factor enforcement: enrollment, authenticator app, email code, recovery code, lockout, and protected-route/API gating                                                                                                                                                                                                                                                                 | Critical | Auth service, route, guard, and manual login-flow tests                                                             | Targeted two-factor, session-guard, module, and auth-flow tests; Playwright E2E `e2e/two-factor-login.spec.ts` (TOTP enrollment, verify, recovery, gating — the enrollee Bob is finance VIEWER, so post-enrollment/verify he lands deterministically on `/admin/payments` per #2090 D-D3); manual browser check for email-code enrollment                                                                                                                                                                                                                                                                                   |
| Auth/access control        | Member/admin                         | Post-login landing (#2090): after sign-in a member with admin access lands on their first accessible admin page (`getFirstAccessibleAdminHref(matrix) ?? /dashboard` — an admin whose overview area is denied still lands on another admin page, never a 403 loop), everyone else on `/dashboard`; a per-member `Member.postLoginLanding` preference (`MEMBER_DASHBOARD`/`ADMIN_DASHBOARD`/null) overrides the default; a genuinely explicit callbackUrl always wins, but a flow-materialised default (2FA detour, provider return URL) does not; applies to credential, magic-link, and Google sign-in. When a 2FA challenge is open the detour's callbackUrl carries ONLY a genuine explicit deep link — the default landing is re-resolved server-side at `/login/enroll` and `/login/verify` from the fully-authed session, so the post-detour destination is deterministic (never a raced post-signIn resolver fetch baking a stale `/dashboard` default); the profile toggle shows only to members with an accessible admin page | High     | Resolver unit, redirect-helper, endpoint, login-form flow, and page-render tests | Targeted `post-login-landing` (full preference × callbackUrl × admin-access matrix, demoted-admin safety, open-redirect rejection), `auth-redirect` (`getExplicitCallbackUrl`), `api/auth/post-login-landing/route` (auth gating + session resolution), `login-form-landing` (2FA gate checked first: detour carries only a genuine deep link, straight-navigate resolve otherwise), `two-factor-detour-landing` (the `/login/enroll` + `/login/verify` pages as the authoritative post-detour resolution site: admin→first admin page, deep-link wins, plain member→`/dashboard`), `login-form-google` (Google returns to `/login` or explicit deep link), `login-page-session-redirect` (authenticated self-heal incl. admin role-default resolution to the first accessible admin page, MEMBER_DASHBOARD preference, explicit-callbackUrl precedence, and the 2FA detour carrying no baked default), `profile/post-login-landing/route` (write route: access-gate 403, changed-value persistence + audit before/after, unchanged-value no-op, invalid-enum 400, GET value + canChoose), and `member-merge` (preference never filled from the loser) Vitest suites |
| Auth/access control        | Anonymous                            | Optional public Addy address-autocomplete proxy is unavailable when its module is off and manual entry still works                                                                                                                                                                                                                                                                               | High     | Feature-route/proxy tests plus address component fallback tests                                                     | Targeted feature-route, proxy, public endpoint, and address component tests; no live Addy calls                                                                                                                                                                                                                                                                                                                                                                                              |
| Privacy/analytics          | Anonymous visitor                    | Google Analytics remains off until the admin Analytics module is enabled, a GA4 measurement id is configured, and the visitor accepts the consent banner                                                                                                                                                                                                                                         | Medium   | Component, CSP, module-readiness, and manual browser/network checks                                                 | Targeted analytics consent, Admin Modules, CSP tests; manual public website and public account-page check for no GA calls before consent and collect calls only after accept                                                                                                                                                                                                                                                                                                                 |
| AI help assistant          | Member/admin/finance                 | Grounded AI help (`POST /api/help/chat`, #2211): the assistant answers only from the trusted per-page help corpus for the DB-verified effective surface (a claimed admin/finance surface downgrades to member unless the member truly holds it), client page-state reaches only the untrusted user turn, and the route degrades to a structured fallback (module_off / not_configured / budget_exhausted / unavailable) rather than a 404 when the module is off, the Anthropic key is missing, the monthly spend cap is hit, or the provider/metering is unavailable. A monthly NZD spend cap hard-stops paid calls; usage is metered per call (question text never stored) and a metering circuit breaker stops spend when it can no longer record. **No browser coverage by design** (no key in CI; a real paid call is non-deterministic and costs money). | Critical | Route gate-order, SDK-taxonomy, metering/cost/budget, and admin-settings/usage Vitest suites (SDK + provider mocked) | Targeted `api/help/chat/route` (gate-order matrix, limits-before-parse, surface downgrade, both-outcome metering), `anthropic-client` (prompt layout, cache_control on block 2 only, page-state in user turn only, angle-bracket strip, error taxonomy), `ai-assistant-usage` (NZ month key, cost ceil/min-1/fail-expensive, budget boundary, fail-closed, circuit breaker), and the two admin-route suites; see [`E2E_PLAYWRIGHT.md`](E2E_PLAYWRIGHT.md) "Not covered by browser tests" |
| Auth/access control        | Member                               | Member cannot access other member booking/family/payment data                                                                                                                                                                                                                                                                                                                                    | High     | API route tests and service ownership tests                                                                         | Targeted Vitest route/service suites                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Lodge operations           | Admin + unauthenticated display      | Lobby display pairing: module flag off → hard 404; flag on → admin creates a device, the unauthenticated TV shows a single-use code, the admin binds it, and the TV claims a hashed display token and flips to the active board unattended                                                                                                                                                       | High     | Route/auth unit tests plus Playwright E2E                                                                            | Targeted lodge-display-auth and display-pair-routes suites; Playwright `e2e/lobby-display.spec.ts` (flag gating, create, pair handshake, active board)                                                                                                                                                                                                                                                                                                                                        |
| Lodge operations           | Admin + paired display               | Lobby display revocation: revoking a device rejects its token on the next request and returns the TV to the pairing screen; lastSeenAt never updates on a rejected request                                                                                                                                                                                                                       | High     | Route/auth unit tests plus Playwright E2E                                                                            | Targeted display-pair-routes AC6 tests; Playwright `e2e/lobby-display.spec.ts` (revoke → pairing screen)                                                                                                                                                                                                                                                                                                                                                                                      |
| Lodge operations           | Paired display (two-lodge club)      | Lobby display lodge scoping: a device bound to lodge B renders lodge B's board and its display-state payload never contains another lodge's bookings, guests, chores, or config; guest names leave the serialiser privacy-reduced (minors never individually named)                                                                                                                              | High     | Serialiser/route unit tests plus multi-lodge Playwright E2E                                                          | Targeted lodge-display-state privacy/scoping matrix; Playwright `e2e/multi-lodge/lobby-display-scoping.spec.ts` (lodge B header, no lodge A content)                                                                                                                                                                                                                                                                                                                                          |
| Lodge operations           | Admin (lodge edit)                   | Custodian bed hold (#2286): a `HutLeaderAssignment` given a `bedId` takes that bed out of the bookable AND allocatable pools for every night from start to end inclusive, with no `Booking` and no `BedAllocation` row anywhere; it is counted as an occupant (so `occupiedBeds + availableBeds === lodgeCapacity` holds), refused at every placement chokepoint with its own `CUSTODIAN_HOLD` category, and released again by an explicit `bedId: null` PUT that keeps the assignment | Critical | Unit/integration only, by decision — see the note below the table                                                    | Targeted `custodian-occupancy`, `custodian-capacity`, `custodian-allocation-guards`, `custodian-assignment-validation`, `custodian-hut-leaders-route`, `custodian-board-and-auto-allocation`, `custodian-write-path-contract` suites; `admin-bed-allocation-range` custodian cases; `bed-allocation-lifecycle` write-time-exclusion cases; `bed-allocation-room-table` custodian band; `hut-leaders-error-placement` release/confirm cases |
| Lodge operations           | Paired display (public wall)         | Custodian slot on the lobby TV (#2286): the footer names the custodian in residence today ONLY for a bed-holding assignment, uses the fixed role word for every club, reports a COUNT on a handover night (two custodians, two beds), and NEVER individually names a minor-age custodian at any name granularity — nor anyone under COUNTS_ONLY; the whole read is skipped when the `hutLeaders` module is off                                                                        | High     | Unit only, by decision — see the note below the table                                                                | Targeted `custodian-display` (serialiser: bed gate, granularity, minor suppression, handover count, module gate) and `display-screen` (footer: fixed role word, un-named case, configurable label never leaks) suites                                                                                                                                                                                     |
| Lodge operations           | Admin (guided setup)                 | Lobby Display guided setup wizard (#2249): `/admin/display/setup` stays reachable while the `lobbyDisplay` module is OFF (exact-match feature-route exemption) while every neighbouring display page and the whole display API still 404; the six step gates are re-derived from live server truth in the signed-off order module → boards → board → config → pair → done; "pick the board" verifies only on a live screen bound to a named board and "done" only once that screen's `lastSeenAt` is set; the config quick-set posts the WHOLE `displayConfig` so keys it does not render survive; the module step never offers a support-area write to a lodge-only admin; the two steps that wait on the TV re-read server truth on a bounded interval and stop on verify/unmount, so the wizard completes in one sitting; a retry after a failed pairing re-arms the SAME screen record rather than creating another; and the cursor persists under the allowlisted `display` id on the unchanged finance gate | Medium   | Config, route-guard and component unit tests (no browser coverage yet) | `src/config/__tests__/feature-routes.test.ts` (exemption + fail-closed neighbours), `src/app/(admin)/admin/display/setup/__tests__/*` (step gates, unresolved-lodge blocking, quick-set payload, pairing order, wait-poll under fake timers, single-device retry), `src/app/(admin)/admin/display/__tests__/display-setup-entry.test.tsx` (hub lead-vs-ordinary card, Help entry), `src/app/api/admin/integrations/wizard-progress/__tests__/route.test.ts` (display id under the finance gate) |
| Auth/access control        | Admin/finance/lodge                  | Role boundaries for full admin, the editable seeded roles (read-only admin, booking officer, membership officer, Treasurer, content manager, finance viewer), custom definition-backed roles, and lodge kiosk; access-role definition management is Full-Admin-only with delete blocked while assigned                                                                                           | High     | Permission-matrix, API route, page guard, and navigation tests                                                      | Targeted admin-permissions, access-role-definitions, admin-access-roles-route, session-guard, access-role UI, finance-auth, and sidebar/nav tests; Playwright E2E `e2e/admin-roles.spec.ts` (one persona per seeded bundled role, in-area render vs out-of-area redirect) and `e2e/access-role-management.spec.ts` (Full-Admin create → edit → assign a custom definition-backed role, asserting the member's Access Roles badge and the role's holder count); manual role checks on staging |
| Lodge operations           | Admin/lodge/hut leader/staying guest | Kiosk week view defaults to a seven-day counts-only overview, respects kiosk date ranges, and opens the existing day operations view for arrival, departure, and roster work                                                                                                                                                                                                                     | Medium   | API route tests, component/page tests, and manual kiosk smoke                                                       | Targeted `phase8-hut-leader-kiosk`, `kiosk-week-view`, and `kiosk-page-week` Vitest tests; manual portrait and landscape kiosk screenshots verifying inaccessible days are dimmed and the day drill-down still loads `/api/lodge/guests/[date]?scope=lodge-list` plus roster data                                                                                                                                                                                                            |
| Booking/capacity           | Member                               | Create booking with capacity lock and per-guest stay ranges                                                                                                                                                                                                                                                                                                                                      | Critical | Service tests, concurrency tests, manual booking flow                                                               | Targeted booking/capacity suites; Playwright E2E `e2e/booking.spec.ts` (`/book` journey, payment-owed booking holds no bed per #737, duplicate member-night block) and `e2e/stripe-payment.spec.ts` (paid booking occupies its beds)                                                                                                                                                                                                                                                         |
| Booking/capacity           | Member                               | Prevent the same linked member from being booked on the same lodge night in multiple live bookings, with open-existing-booking and future self-removal recovery paths (self-removal is reachable both from the wizard's conflict card and from the booking's own detail page, #2250)                                                                                                                                                                                                                            | High     | Service, route, and booking-flow UI tests                                                                           | Targeted member-night conflict helper, booking quote/create, and guest-removal route tests; `booking-guest-self-removal` eligibility tests (the one rule shared by the page affordance, the conflict card, and the removal service's status gate); `booking-member-night-conflict-messages` copy tests, including that the 409 message never names the other booking's owner, id, or status; 409 **payload** scoping tests (an unentitled requester's conflict row carries no booking id, owner, status, stay dates, or guest id — asserted as a field whitelist, not a spot-check — while the clashing guest themselves, the booking's owner, and the admin conflict-resolution path still receive all of it); `self-remove-from-booking-card` component tests (confirm-before-removal, hidden-not-disabled when ineligible, server refusal surfaced verbatim); manual `/book` duplicate-member flow (the booker is pre-selected by default and can be removed) and manual "open a booking somebody else put you on" flow                                                                                                                                                                                                                                                                                                                                                             |
| Booking/capacity           | Dual-hat admin/Booking Officer       | Dual-hat admins (USER + admin roles) self-book through the member wizard under full member rules (no redirect, no admin gate bypasses); admin-only accounts still redirect to Book on Behalf; Booking Officers (bookings:edit) create and quote on-behalf bookings with the self-target block; unauthorized forMemberId on create/quote/promo-validate rejected (#1442)                          | High     | Route authorization tests plus Playwright journey                                                                   | Targeted `admin-book-on-behalf` vitest suite (dual-hat self-create with member gates, officer on-behalf create, self-target and unauthorized 403/400 paths); Playwright E2E `e2e/dual-hat-booking.spec.ts` (dana books herself via `/book` to the payment step, e2e-admin redirect, booking officer drafts an on-behalf booking via `/admin/book`)                                                                                                                                           |
| Booking/capacity           | Admin (Full Admin / Booking Officer) | Admin date override (#1668): move an in-progress or fully-past booking's dates the normal edit window locks. Date-only; **Shift** freezes every price and keeps the night count while **Recalculate** reprices; an over-capacity target is warn-and-confirm (409 `OVER_CAPACITY_CONFIRM_REQUIRED`), the capacity lock still taken; a Recalculate whose resolved check-in is on/before the Xero lock date is rejected at preview and apply (#1697: 409 `XERO_PERIOD_LOCKED`, fail-closed 503 `XERO_LOCK_DATE_CHECK_FAILED`, Shift exempt) — deliberately conservative for overrides (settled on #1718), while ordinary non-override date edits get a narrow guard that fires only when the edit would queue the check-in-dated invoice update (issued unpaid invoice + dates changing, #1729) with member-facing wording for non-admin actors; identity-only edits are never guarded; every move is audited (`booking.modify.admin_override`) and linked to the latest approved change request; the modify-quote preview mirrors apply; members never see the override control | High     | Service, route, quote-preview, and Playwright journey                                                              | Targeted `booking-edit-policy`, `admin-shift-booking-dates`, `resolve-target-dates-admin-override`, `calculate-modified-pricing-capacity`, `booking-change-request-linkage`, `modify-admin-override`, `modify-quote-shift`, `modify-quote-recalc-override` (also the #1729 ordinary-edit guard cases), `xero-period-lock-guard`, `xero-booking-edit-settlement` (shared narrow predicate), and `modify-override-xero-lock-guard` (override + #1729 ordinary wiring) Vitest suites; Playwright E2E `e2e/admin-override-dates.spec.ts` (member books stayWindow(5); admin shifts it into an in-progress stay then +1 night; shifts it fully-past then again; over-capacity confirm; and a member session sees no override control) |
| Booking/capacity           | Admin (Full Admin / Booking Officer) | Admin retroactive create (#1695): create a booking on behalf of a member with a past check-in (≤365 days back). Admin-only + on-behalf-only flags (`allowPastDates`/`confirmOverCapacity`/`notifyMember`), route-gated + service re-check; guarded by the Xero organisation lock dates (409 `XERO_PERIOD_LOCKED`, fail-closed 503 `XERO_LOCK_DATE_CHECK_FAILED`, skipped when Xero is unconnected — as in E2E); over-capacity is warn-and-confirm (409 `OVER_CAPACITY_CONFIRM_REQUIRED`) with the capacity lock still taken; the member email is an explicit per-create choice recorded in audit metadata (`booking.created_on_behalf`); a retroactive create left on the card path at `PAYMENT_PENDING` cannot silently linger — the admin dashboard "Unpaid Finished Stays" card (#1709, owner option B) counts `PAYMENT_PENDING` bookings with check-out on or before NZ today (organic bookings that cross check-out unpaid surface too) and deep-links to `/admin/bookings?status=PAYMENT_PENDING&checkOutTo=<today>` via the new Check Out range filter; the sidebar Needs Attention menu badges the same queue (#1731) through the shared `src/lib/unpaid-finished-stays.ts` predicate/href helpers | High     | Service, route, helper, component, and Playwright journey                                                          | Targeted `booking-create-past-dates`, `create-past-dates-admin`, `xero-lock-dates`, and `booking-calendar-past-dates` Vitest suites (`admin-dashboard-deeplinks` and `admin-bookings-page` cover the unpaid-finished-stays card and `checkOutFrom`/`checkOutTo` filter; `unpaid-finished-stays`, `admin-pending-counts`, and `admin-sidebar` cover the shared predicate and sidebar badge); Playwright E2E `e2e/admin-retroactive-booking.spec.ts` (admin records a past stay via `/admin/book` → email-choice dialog → "Create without emailing" → 201; a member's `/book` calendar keeps past days disabled; a member POST carrying `allowPastDates` is rejected 403) |
| Booking/capacity           | Admin (Full Admin / Booking Officer) | Book on Behalf — non-member owner (#1935 E9, live-app follow-up #1962): an officer inline-creates a non-login `NON_MEMBER` booking owner on `/admin/book`, or reuses an existing one via suggest-and-pick dedupe (never silent reuse), or records a no-email walk-in whose address is a club-internal `…@no-email.invalid` placeholder; the owner's typed guests are priced at non-member rates (the on-behalf quote returns `isMember:false` and the Playwright journey asserts the quote total equals the exact seeded non-member rate for the booked window, distinct from the member rate), the booking is held `PENDING` (non-member hold), and the per-create email choice controls the owner's `Booking Pending` hold email — a real-email owner receives it on opt-in, a placeholder walk-in owner is never emailed (send is never attempted and `sendEmail` also suppresses placeholder recipients) | Low (test-only) | Service, route, email-suppression, pricing-parity Vitest plus Playwright journey | Targeted `non-member-contact`, `placeholder-contact-email`, `placeholder-recipient-suppression`, and `non-member-pricing-parity` Vitest suites; Playwright E2E `e2e/book-on-behalf-nonmember.spec.ts` (inline create → non-member pricing → real-email owner gets the `Booking Pending` email on opt-in; suggest-and-pick reuse of the same contact; no-email walk-in create with no email to the reserved placeholder domain) |
| Booking/capacity           | Admin (Full Admin / Booking Officer) | Admin capacity hold (#1764): hold/unhold lodge capacity for a `PAYMENT_PENDING` booking via `/api/admin/bookings/[id]/capacity-hold` — status-scoped `capacityHoldingBookingFilter()` disjunct (availability/board/waitlist/stats inherit), advisory-locked capacity re-check with explicit overbook confirm (409 `CAPACITY_EXCEEDED`), unhold refused once naturally holding, every cancel path clears the hold via `RELEASE_ADMIN_CAPACITY_HOLD_UPDATE`, audit rows both ways, non-admin 403                                                                                                                                                                             | High     | Route, filter/status, and cancel-path release tests                                                                   | Targeted `admin-capacity-hold-route`, `capacity-holding-filter` (admin-hold disjunct cases), and cancel-release assertions in the booking-cancel/cron suites                                                                                                                                                                                                                                                                                                                   |
| Booking/capacity           | Admin (Full Admin / Booking Officer) | Admin forward-dated on-behalf overbook (#1767): a future-dated on-behalf create over capacity is warn-and-confirm (409 `OVER_CAPACITY_CONFIRM_REQUIRED` → `confirmOverCapacity: true`, `capacityOverridden` audited with `allowPastDates: false`); route gates the flag to on-behalf (400 without `forMemberId`, 400 with `draft`/`waitlist`, 403 non-admin); the waitlist opt-in falls through to WAITLISTED via the service-only `waitlistIntent`; a non-member hold-eligible (PENDING) party is now admitted and marked, the v1 hard block retired by #1771 (persisted override honoured by the hold cron); member self-books keep the hard block (route + service pins); `/admin/book` keeps full future days selectable (`allowFullDates`, member calendar pin), caps guests by the selected lodge's resolved capacity, and blocks Save-as-Draft for over-capacity parties | High | Service, route, and component tests | Targeted `booking-create-past-dates` (forward-dated describe), `create-past-dates-admin` (#1767 gating cases), and `booking-calendar-past-dates` (`allowFullDates` on/off) Vitest suites; no dedicated E2E (the admin journey shares the retroactive spec's page machinery; over-capacity confirm is pinned at route/service level) |
| Booking/capacity           | Admin (Full Admin / Booking Officer) | Persisted capacity override (#1771): every over-capacity admission (on-behalf create #1668/#1695/#1767, date/batch modify #1668, waitlist force-confirm, confirm-pending-guests overbook #1366, admin capacity-hold #1764) stamps `Booking.capacityOverriddenAt` + `capacityOverriddenByMemberId` (immutable, never cleared on cancel); every payment-time / settlement re-check (`markBookingPaymentSucceeded`, payment links, `cron-confirm-pending`, `charge-saved-method`, `switch-to-internet-banking`, Internet Banking invoice-paid reconcile, group settlement) honours `bookingHasCapacityOverride` and settles/advances to the correct terminal state instead of cancel+refund / 409 / bump; a priced overridden booking no longer self-destructs when payment lands over capacity; DRAFT-scoped re-checks (the `create-payment-intent` DRAFT arm, `confirm-draft`) are exempt (a DRAFT can never carry an override), but the #2265 credit-election arm of `create-payment-intent` is NOT: an admin-released `AWAITING_REVIEW` booking can carry an override, and that arm can settle at $0, so it re-checks capacity and honours the override like every other settle path (refusing with a 409 that leaves the election unspent when there is no override); members can never overbook | High | Set-site + read-site unit pins across services, routes, cron, and the Xero reconcile | Targeted `payment-reconciliation`, `payment-link`, `cron-confirm-pending`, `charge-saved-method-route`, `group-settlement`, `xero-inbound-reconciliation`, `booking-create-past-dates`, `admin-force-confirm-route`, `admin-confirm-pending-guests-route`, `admin-capacity-hold-route`, `admin-shift-booking-dates`, and `issue-2265-draft-credit-election-route` Vitest suites (override-honoured + regression pins); no dedicated E2E (pinned at service/route level) |
| Payment/refund/credit      | Member/admin                         | Promo usage caps count only real benefits (#2299): a `PromoRedemptionAllocation` row exists only where the application moved money (`discountCents > 0`, `priceAdjustmentCents != 0`, or `freeNightsUsed > 0` — a price-RAISING fixed-nightly application counts), and all three caps (uses per member, total redemptions via `PromoCode.currentRedemptions`, unique members) count those rows and nothing else; `normalizeAllocations` is the single write-time choke point and `BENEFICIAL_PROMO_ALLOCATION_FILTER` guards every cap query, so a legacy all-zero row stops consuming a slot before the repair migration runs. The `PromoRedemption` row is still written for any application with eligible guests (audit + reporting), so archive-or-delete counts redemptions, not allocations. A reprice RELEASES the slot it held in the same transaction that removes the benefit, and the total-uses check discounts the repriced booking's OWN allocation rows (`excludedBookingRedemptionCount`) — without it a booking on a code's last slot fails its own reprice and the member is billed the whole discount back for a date shift. All four modification paths row-lock the promo code before their first cap read and re-read `currentRedemptions` under that lock — the four reprice call sites through `lockAndRefreshPromoCodeUsage`, whose returned (refreshed) object must be the one validated. A `SET_PRICE` application whose per-guest adjustments net to exactly zero is deliberately no use. Migration `20260731130000` repairs existing data (pure idempotent DML; re-run once after a blue/green drain). | Critical | Pricing, persistence, concurrency, migration, and admin-surface Vitest | Targeted `promo-zero-benefit-usage-caps` (benefit test, every zero-benefit shape, trigger-ordering, counter symmetry, member-facing status, repair SQL), `promo-reprice-cap-exclusion` (the excluded-booking arithmetic, the shared choke point, two reprice paths driven end to end, and a source contract over all four call sites), `promo-modify-cap-lock` (swap lock order), `admin-promo-codes-route` and `admin-promo-redemptions-route` (benefit-free counts, cap numerators, archive-vs-delete), plus `advisory-lock-guard` (row-lock site inventory) Vitest suites; no dedicated E2E (pinned at service/route level) |
| Data integrity (raw SQL)   | Member/admin (booking creation, promo pricing) | Raw SQL cannot lie about its result shape (#2289): `$queryRaw<T>` is an unchecked cast — raw SQL returns the PHYSICAL column names while the generic declares the Prisma ones, so a disagreement arrives as `undefined` rather than as an error. That silently disabled a promo total-redemption cap (`undefined !== null` true, `n > undefined` false) and zeroed the FREE_NIGHTS discount (`?? 0`) at booking creation in a deployment whose columns predated two rename migrations, while the quote path showed the member a discount they were then charged without. Every row lock now takes its lock with `$executeRaw` on a CONSTANT and reads its data back through the Prisma model under that same lock (booking-create promo, member photo x2, club theme, bed-allocation room, promo modification); the one statement that genuinely cannot be a model read — the rate limiter's atomic `CASE … RETURNING` upsert — validates its rows with `decodeRawRows`, so a renamed column can no longer make `Number(undefined)` NaN and stop the limiter limiting. No `$queryRaw<…>` generic and no `SELECT *` remain in non-test code under `src/`, `scripts/` or `prisma/`, in either call form (tagged template or `Prisma.sql` composition); tests are deliberately exempt from both guards — `concurrency-lock-races.realdb.test.ts` reads raw counts on purpose, and a wrong shape there fails the test rather than mispricing a booking. | Critical | Decoder unit tests, a promo shape regression test, a source-contract inventory, and an ESLint rule | Targeted `raw-sql-rows` (shape pass, missing column, wrong type, row index, no values in the message, BigInt/`COUNT(*)`/numeric-string handling), `issue-2289-promo-lock-read-shape` (hands the transaction the lying snake_case row AND the true model row at once: the cap must fire and the FREE_NIGHTS discount must apply), `rate-limit` (a renamed `count` column must not stop the limiter limiting), and `raw-sql-shape-guard` (per-file raw-READ inventory, decoder-or-documented-opt-out, no type argument, no `SELECT *`, every `FOR UPDATE` on `$executeRaw` over a constant) Vitest suites, plus `npm run lint` for the `no-restricted-syntax` rules; no dedicated E2E (pinned at unit/source-contract level) |
| Payment/refund/credit      | Member/admin                         | Edit-path credit election + promo surfacing (#2266, epic #2245 E2): the modify preview returns the booking OWNER's `availableCreditCents` and treats a credit-only request as price-preserving (identity echo — no repricing, no capacity check); `applyCreditCents` on modify stores/clears the #2265 election with the pay-step-honoured status guards (DRAFT/AWAITING_REVIEW/PAYMENT_PENDING only — never hold-rail PENDING, captured money, or organiser-settled — raw amount stored, clamping stays at the consumer, $0-settle drops the moot request); guest-targeted promo beneficiaries bind by `promoGuestIds` for existing guests (stale ids 400 at preview and apply alike; positional indexes only for to-be-added guests via `promoAddedGuestIndexes`), resolved through one shared function so preview and apply can never disagree; members edit their own drafts (Resume) with no change fee and no hold stamp, lifecycle set frozen so admin draft edits still skip lifecycle rules; a member draft edit that trips the no-adult rule parks the booking to AWAITING_REVIEW with `draftExpiresAt` nulled (create parity), and the confirm-draft / create-payment-intent doors refuse any DRAFT with an unresolved review; change requests refuse DRAFT (a draft is member-editable directly); admin date-only override refuses credit input (including falsy 0) | High | Policy, lifecycle, and route Vitest | Targeted `issue-2266-edit-credit-policy` (resolveCreditElectionUpdate matrix, member-draft edit policy, frozen lifecycle set, DRAFT hold guard, review parking, effective-price intent sweep, notice audience), `issue-2266-draft-review-doors` (both doors refuse an unresolved review), `admin-booking-review-route` (legacy DRAFT reject robustness), `promo-beneficiary-binding` (id binding + drift refusal), `modify-quote-edit-credit-promo` (credit-only echo, owner balance, DRAFT change-fee guard, promo id threading + stale-id 400, override refusal), `modify-credit-election-input` (schema pass-through, 0-clear, negative/fractional 400, override refusal), `edit-booking-panel-credit-honesty` (untouched-election stability, clamped applied-credit display, change-fee line), and `booking-change-requests` (DRAFT refusal) Vitest suites |
| Booking/capacity           | Member/admin                         | Waitlist, offer expiry, force-confirm, bump/cancel                                                                                                                                                                                                                                                                                                                                               | High     | Service tests and cron tests                                                                                        | Targeted waitlist tests plus safe cron unit tests; Playwright E2E `e2e/waitlist.spec.ts` (full-night refusal → WAITLISTED, admin force-confirm/overbook, member offer accept, offer/expiry admin state). Offer creation + expiry run only on the in-process scheduler (no HTTP endpoint) and stay cron-unit-tested                                                                                                                                                                           |
| Booking/capacity           | Admin / public group joiner          | Finished-stay card-obligation side doors (#1723): a past-dated waitlist force-confirm landing `PAYMENT_PENDING` is allowed but flagged at creation (`unpaidFinishedStay` response field, `createdUnpaidFinishedStay` audit metadata, amber `/admin/waitlist` card); a settled (`CONFIRMED`/`PAID`/`COMPLETED`) past stay whose upward-modification delta was never collected counts on the "Finished Stays With Unpaid Additions" dashboard card, the "Unpaid Stay Additions" sidebar badge, and the bookings list `additionalOwed=owed` filter (shared predicate in `src/lib/unpaid-finished-stays.ts`, disjoint from the `PAYMENT_PENDING` queue); a group whose organiser stay has fully ended (check-out on or before NZ today) leaves the joinable set (summary `isJoinable` false, member join 409, non-member request 409 `GROUP_STAY_ENDED`, emailed-token verify `not_joinable`) | High     | Helper, service, route, and component tests                                                                         | Targeted `unpaid-finished-stays` (predicate/href drift guards incl. disjointness), `admin-pending-counts`, `admin-dashboard-deeplinks` (card render/hide + count predicate), `admin-sidebar` (badge show/hide + deep link), `admin-bookings-page` (`additionalOwed` filter composition), `admin-force-confirm-route` (flag true for past-dated and checks-out-today; false for future-dated, $0 → PAID, parked-for-review), `group-booking` (`hasGroupStayFullyEnded` incl. the NZ-day rollover edge), `group-join-stay-ended` (all three service gates + gate precedence), and `group-bookings-route` Vitest suites |
| Booking/capacity           | Member                               | Member whole-lodge request (#2263, epic #2245): a signed-in member asks for sole occupancy from `/book/whole-lodge` (dates, approximate headcount, group description — no guest names, no availability calendar, no price, no capacity pre-check). The row is an ordinary `BookingRequest` entering at `VERIFIED` with `exclusivityRequested` and `requestedByMemberId`, holding NO capacity. Officer approves from the shared queue with a priced headcount (or the price override when no season covers the dates), producing a **CONFIRMED** booking owned by the member, `wholeLodgeHold` stamped, a PENDING INTERNET_BANKING payment, and **no** payment link or token email. That receivable is then INVOICED post-commit like every sibling path: the Xero booking-invoice op plus the #1620 applied-credit allocation op when the `xeroIntegration` module is on, or the delivery-locked `admin-whole-lodge-manual-invoice` alert (carrying the member's payment reference) when it is off; the member's confirmation states the amount **owing** plus that reference and never "Total Paid". Approve replays idempotently via `convertedBookingId` — no second booking, payment, invoice or email, and the replay reports the COMMITTED figures. Member sees the result under **My requests** on My bookings and may **withdraw** while pending (refused, 409, for any row holding capacity). | Critical | Playwright E2E + Vitest service/route | `npx playwright test e2e/whole-lodge-request.spec.ts`; `npx vitest run src/lib/__tests__/school-booking-request.test.ts src/lib/__tests__/member-whole-lodge-request-service.test.ts` |
| Privacy/disclosure         | Member                               | Whole-lodge request uniform response (#2263, ADR-001 decision 6 / D11): the acknowledgement for a schema-valid submission is **byte-identical** whether the requested nights are clear, ordinarily full, or already exclusively held for another group — asserted as buffers, across three seeded worlds and three member sessions. The handler issues no availability, occupancy, season or pricing query at all, so there is no branch to time. "My requests" carries a strict-allowlist DTO whose status is one of four words (raw `PRICED`/`QUOTE_SENT`… never reach a member), and a member-origin decline persists no officer note and emails one fixed generic sentence. | Critical | Playwright E2E (three seeded worlds, byte-compare) + Vitest structural/DTO | `npx playwright test e2e/whole-lodge-request.spec.ts`; `npx vitest run src/lib/__tests__/member-whole-lodge-requests.test.ts src/app/api/booking-requests/whole-lodge` |
| Booking/capacity           | Admin (Full Admin / Booking Officer) | Whole-lodge queue and hold grant (#2263): the shared booking-requests queue badges **Member** and **Whole lodge requested** (the latter for SCHOOL rows too), offers an admin-only per-night availability + conflict strip (`/api/admin/booking-requests/[id]/hold-conflicts`, `requireAdmin`, never reachable from member context), and hides the quote/price/hold controls for member rows — which the service layer also refuses with 409, so hiding is UX and the 409 is the guarantee. Approving surfaces overlapping capacity-holding bookings to the **admin only** and never displaces them (ADR-001 decision 1), and prunes any per-bed rows with the #2285 audit. | High | Playwright E2E + Vitest route/service | `npx playwright test e2e/whole-lodge-request.spec.ts`; `npx vitest run src/lib/__tests__/api-route-boundaries.test.ts src/lib/__tests__/exclusivity-request-write-sites.test.ts` |
| Payment/refund/credit      | Member/admin                         | Stripe payment success/failure, saved card, refund, member credit                                                                                                                                                                                                                                                                                                                                | Critical | Unit/service tests with Stripe mocked                                                                               | Targeted payment, refund, credit suites; Playwright E2E `e2e/stripe-payment.spec.ts` covers **only** test-mode payment success + decline (skips without test-mode keys). **No browser coverage of refund, cancellation-with-refund, saved card, or member credit** — those stay Vitest/service-only for now; a cancellation-with-refund browser spec is tracked in #1373                                                                                                                     |
| Payment/refund/credit      | Admin (Full Admin / Booking Officer) | Outstanding additional payment (#2350): an upward booking change after payment leaves an uncollected additional payment. It is visible on the bookings list (**Partly paid** + amount due, the lifecycle chip untouched), on the booking page (a read-only **Additional payment outstanding** panel: amount, age, failed-charge state, when the member was last emailed), in the needs-attention queue (upcoming **and** finished stays, split label, one owed-filter link), and on the reports/finance money surfaces (**Booked Revenue** vs **Outstanding Additions**). The member is chased automatically at +3 days and 2 days before check-in, the pre-arrival reminder names the amount, and an admin can re-send on demand; automatic and manual sends share one 60-minute cooldown and one pair of `Payment` stamps, read relative to the current obligation, so nothing double-sends and a later change is still chased from scratch. Never auto-cancels; stops at check-out | High     | Service, route, component, and Playwright journey                                                                   | Targeted `cron-additional-payment-reminders`, `additional-payment-resend`, `admin-additional-payment-reminder-route`, `booking-additional-payment-panel`, `admin-permissions` (panel gating), `admin-bookings-page`, `booking-history`, `admin-pending-counts`, `admin-sidebar`, `admin-dashboard-deeplinks`, `admin-reports-route`, `finance-booking-metrics`, and `finance-dashboard-page` Vitest suites; Playwright E2E `e2e/additional-payment-chase.spec.ts` (owed filter → list markers → panel → re-send + captured email → cooldown refusal). The card-payment completion of an additional payment stays Vitest/service-only (Stripe-gated) |
| Payment/refund/credit      | Member/admin                         | Cancel a paid booking and receive the settlement (card refund or member credit) with correct status transitions                                                                                                                                                                                                                                                                                  | Critical | Service/route tests plus manual staging                                                                             | Targeted booking-cancel/settlement/refund suites; Playwright E2E `e2e/booking-cancel-refund.spec.ts` (member cancels a paid booking with the **account-credit** method → booking CANCELLED with the Cancellation Outcome credit row, and the admin payments ledger shows the Account credit settlement — guards the P0-1/P0-2 cancel-refund fixes). **Card-refund** (a real Stripe refund) stays Vitest/service-only (Stripe-gated)                                                          |
| Booking/capacity           | Admin                                | Approve a review-flagged booking, then allocate its guests to specific beds (auto + manual)                                                                                                                                                                                                                                                                                                      | High     | Service/route tests plus manual staging                                                                             | Targeted bed-allocation service/route tests (module enabled on the staging stack); Playwright E2E `e2e/bed-allocation.spec.ts` (admin approves a review-flagged booking via the approvals panel, then manually allocates its guest to a specific bed via the Select + Allocate path — a MANUAL draft chip that Approve Visible flips to Approved)                                                                                                                                            |
| Booking/capacity           | Admin                                | Move existing allocation chips to another bed without changing their original NZ lodge nights (#2366) | High | Planner, service, route, pointer/keyboard feedback, cancellation, conflict and Playwright coverage | `bed-allocation-board-move` (first-visible proxy, later-chip single night, same-bed no-op, pointer preview, keyboard announcement and cancellation copy), `admin-bed-allocation` (destination lock-first, under-lock original-date read, server no-op/no audit, atomic bulk conflict, shared-double promotion and custodian refusal), `bed-allocation-move-route` (strict date-free request and auth/module gate), plus `e2e/bed-allocation.spec.ts` (cross-column pointer preview + Escape sends no PATCH; date-free PATCH preserves the persisted night) |
| Booking/capacity           | Admin                                | Allocate and confirm one booking's beds from inside the booking, and keep the panel invisible to members (#2252) | High     | Service/route/component tests plus Playwright E2E | `admin-bed-allocation` (the `bookingId` approve selector is sufficient alone, never widens, and the room-request lock flips back off), `bed-allocation-approve-route` (`{ bookingId }` accepted, `targetId` audited against the booking, strict schema, module-off 404, non-admin refusal), `booking-bed-allocation-panel` (payload filtered to this booking, the shared range dialog opened prefilled with its lock warning, Confirm posts `{ bookingId }` and never a range, the last-approved removal warning, hold and non-allocatable states, a 61-night stay paged with both windows labelled, view-only gating), `admin-permissions` (identity contract + a source pin on the render gate); Playwright E2E `e2e/bed-allocation.spec.ts` (a move re-drafts an approved row, the booking page's Bed allocation card confirms it, and the member's own booking shows no such card) |
| Payment/refund/credit      | Member/admin                         | Internet Banking/Xero invoice settlement distinct from Stripe                                                                                                                                                                                                                                                                                                                                    | Critical | Service tests with Xero mocked                                                                                      | Targeted Xero booking invoice/reconciliation tests; Playwright E2E `e2e/internet-banking.spec.ts` (switch a card booking to Internet Banking with Xero absent — reference shown, invoice queued not sent, no crash)                                                                                                                                                                                                                                                                          |
| Webhook replay/idempotency | Stripe/Xero/SES                      | Valid, duplicate, malformed, oversized, and wrong-signature payloads                                                                                                                                                                                                                                                                                                                             | Critical | Route tests with fake signed payloads                                                                               | Targeted webhook route tests, no live provider calls. All five payload classes are covered per handler (issue #1133): `stripe-webhook-alerts.test.ts`, `xero-webhook-route.test.ts`, `ses-sns-webhook.test.ts`, plus DB-level replay dedup in `xero-sync.test.ts` and the SES claim-release retry path                                                                                                                                                                                       |
| Cron rerun/recovery        | Scheduler                            | Payment recovery, pending confirmation, waitlist, Xero retry, email retry                                                                                                                                                                                                                                                                                                                        | High     | Unit/route tests with local DB or mocks                                                                             | Targeted cron tests; never use production `CRON_SECRET`                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| Membership lifecycle       | Applicant/member/admin               | Application, nomination reminders, admin refresh/replacement, approval, cancellation, archive, delete                                                                                                                                                                                                                                                                                            | High     | Service tests, cron tests, and admin route tests                                                                    | Targeted membership/lifecycle suites; Playwright E2E `e2e/membership-application.spec.ts` (public submit → both nominators agree via `/nominations/<token>` → admin approval → applicant exists as a member). Nomination-reminder cron stays unit-tested                                                                                                                                                                                                                                     |
| Membership lifecycle       | Admin/member                         | Configure seasonal membership types with allowed age tiers and Xero group rules, assign member seasonal types with apply-from date plus guarded preview/reason/audit, roll assignments forward, enforce booking rate/block policy, and surface effective subscription status without losing raw history                                                                                          | High     | Schema, migration/backfill, admin route, service, pricing, booking gate, and navigation tests                       | Targeted membership type seed/API/sidebar/member-assignment suites plus membership-type policy, Xero rule validation, booking quote/create/modify, group join, and subscription-status tests                                                                                                                                                                                                                                                                                                 |
| Hut rate re-key (#1930, E4) | Admin/member/non-member | Price hut nights by membership type (`MembershipTypeSeasonRate`), not a member/non-member boolean; day-one fan-out backfill keeps every existing (guest, night) price and Xero item code byte-identical; group discount substitutes the configured type only for true non-members while members keep their own rate and TYPE_POLICY_FORCED members are excluded; missing rate hard-throws + setup warning; Xero item codes re-key by type; BookingGuest snapshot recomputes on reprice, untouched on locked nights | Critical | Schema/migration, partial-index manifest, engine, resolver, ~16 caller sites, Xero mappings + line builders, seeds, setup-readiness, byte-identical regression, invariant, rateSource matrix, and missing-rate blast-radius tests | Byte-identical old-key vs new-key regression over seeded (guest, night); NON_MEMBER_RATE/BLOCK zero-own-rows invariant; group-discount rateSource matrix (non-FULL MEMBER_RATE member keeps own rate; TYPE_POLICY_FORCED excluded); missing-rate hard-throw + waitlist-promotion fail-safe; snapshot overwrite-on-reprice vs locked-night; Xero item-code resolver (tier/flat/NULL-snapshot fallback); `db:check-drift` incl. partial-index set-equality; multi-lodge + Playwright booking/waitlist/invoice suites |
| Fee configuration | Membership/Finance admin | Keep public membership listings hidden until reviewed; create non-overlapping effective annual/entrance schedules; resolve family billing exceptions | High | Migration/backfill, permission split, integer cents, inclusive boundaries, overlap and compatibility fallback | Targeted authoritative-fee, membership-type, family, permission-map, Xero entrance, and admin UI tests |
| Subscription billing | Treasurer/Membership officer/member | Preview annual per-member/per-family/no-invoice charges; explicitly confirm; queue newly approved members; persist/adopt AUTHORISED GST-inclusive Xero invoice; email and retry without duplicate; expose exceptions/conflicts | High | Focused planner, confirmation-service, route/component permission/staleness, nomination-hook, outbox, mocked Xero create/adopt/mismatch/email-retry, migration, and disposable-Postgres checks | Unit/service coverage includes March- and December-year-end bounds, proration/rounding, missing assignment/fee/family/recipient/explicit mapping, late family member suppression, immutable mapping/coverage replay, provider partial recovery and non-AUTHORISED conflict, shared-family paid reconciliation, exception dedupe, and non-blocking approval warnings; disposable Postgres verifies migration/drift and concurrent uniqueness; no live-provider or browser claim |
| Admin member management    | Admin                                | Members and Subscriptions share four role-independent Access states and stable Xero group tones; Members offers name + Open links to a read-only detail page for membership-view users; finance-only subscription viewers get plain member text; member detail ignores legacy `?edit=true` and preserves explicit per-section Edit → Save/Cancel, default-collapsed groups, `#account-credit`, Xero connection gating, and group-scoped PUT payloads | High | Component (jsdom), helper, permission, payload-builder, API route, and Playwright navigation tests | Targeted `member-status-opening`, `xero-contact-group-tone`, Members `access-role-ui`, `admin-member-detail-groups` forged-query guard, subscriptions route serialization, existing member edit/payload suites; Playwright `admin-member-detail.spec.ts` covers keyboard Open + read-only landing and `admin-roles.spec.ts` covers finance-only subscription link suppression |
| Committee administration   | Admin/member/public                  | Configure committee master roles with role email aliases, link members to roles, edit hidden-by-default assignment presentation flags, and publish privacy-controlled committee/contact records that do not expose member email addresses publicly and route contact mail to role email before member-email fallback                                                                             | High     | Schema, migration/backfill, admin route, member detail, seed, audit, public API, contact route, and component tests | Targeted committee role/assignment API tests, seed defaults, member detail integration, public committee privacy tests, contact routing fallback tests, and public committee/contact component tests                                                                                                                                                                                                                                                                                         |
| Family/dependents          | Member/admin                         | Adult invitations, dependents, inherited email, age-up                                                                                                                                                                                                                                                                                                                                           | High     | Service tests and UI route tests                                                                                    | Targeted family/dependent tests                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Partner relationship (#1742) | Member/admin                       | Declare partner by email (request→confirm), one-step declaration by a family-group admin for a no-login adult, withdraw/decline/dissolve (a dissolve also sweeps the pair's future shared double-bed second-occupant allocations back to the awaiting-allocation queue, #1756), member-side cancel of an unclaimed declared-partner invitation (own `createPartnerLink` tokens only, audited, #1754), admin assign/remove on the member detail page, partner link formed on a declared partner-invite claim, one-confirmed-partner invariant                                                                                                                | High     | Service tests, route tests, and create-group/claim integration tests                                                | Targeted `member-partner-link`, `member-partner-link-routes`, `family-group-create` (declarePartnerLink), `partner-invite-token`, and (sweep) `bed-allocation-lifecycle` + `partner-share-sweep-routes` suites; DB guards (pair CHECK + partial unique indexes) verified against a real Postgres during migration validation; Playwright E2E `e2e/double-bed-sharing.spec.ts` exercises an admin-assigned confirmed link surfacing as an edit-panel sharing candidate                                                                                                                                                                                                                                     |
| Partner-shared double-bed capacity (#1745/#1746) | Admin | Reserved headroom resolution (doubles, capacity-cap interplay), partner-shared admission (full-by-beds admits an eligible sharer up to the double count then rejects; ordinary guests never take reserved slots; mutual/duplicate sharers rejected), admin-only gating of `partnerSharedGuests` at route+service, edit-panel partner quick-add candidates, capacity-card breakout, board second-occupant placement + orphan auto-promote (#1701/#1743), stale-pair sweep on dissolve/deactivation/tier change with occupancy-derived slot accounting intact post-sweep (#1756) | High | Unit + route tests + Playwright E2E | `capacity-partner-shared`, `booking-modify-partner-shared`, `double-bed-sharing` (candidates), `admin-bed-allocation` (payload), and (sweep) `bed-allocation-lifecycle` + `partner-share-sweep-routes` suites; Playwright E2E `e2e/double-bed-sharing.spec.ts` (admin edit-panel partner quick-add renders and adds a confirmed partner; on a future window filled to exactly the base capacity by a capacity-holding filler the modify-quote preview admits the confirmed partner into the **reserved** double slot while an ordinary guest is refused, and removing the only double — headroom→0 — refuses the same partner, so the raise is reserved and **bounded** by the double count; a fabricated non-partner pairing is refused with the confirmed-partner reason and a partnerless booking offers no candidates; the board places the partner as a shared double's second occupant then auto-promotes the orphan when the primary allocation is removed). Exhaustive per-night admission/rejection boundary cases stay in the `capacity-partner-shared` unit suite |
| Xero/SES failure           | Admin/operator                       | Xero outbox failure/retry, SES suppression/failure visibility                                                                                                                                                                                                                                                                                                                                    | High     | Service tests with provider mocks                                                                                   | Targeted Xero outbox/email retry tests                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| UI/UX journey              | Member                               | Dashboard summary cards drill into bookings, anchored profile account credit/promo sections, and the singular payment-owed booking when applicable                                                                                                                                                                                                                                               | Low      | Server component render tests plus manual member-dashboard smoke                                                    | Targeted dashboard/profile page render tests for card `href`s and profile anchor IDs; manual `/dashboard` light/dark smoke                                                                                                                                                                                                                                                                                                                                                                   |
| UI/UX journey              | Admin                                | Setup opens with KPIs first, required blockers directly underneath, hub drill-down cards before detailed readiness checks, and Finance Report Mappings collapsed inside `/admin/setup/finance`                                                                                                                                                                                                     | Low      | Component/server render tests plus manual setup-page smoke                                                          | Targeted setup page order, hub-card, contextual-help, and finance drill-down render tests; manual `/admin/setup` plus `/admin/setup/finance` light/dark smoke                                                                                                                                                                                                                                                                                                                                 |
| UI/UX journey              | Admin                                | Long admin form save feedback scrolls the admin scroll container or dialog body to top on success, and focuses the first top error banner on validation failure                                                                                                                                                                                                                                  | Low      | Hook unit tests plus manual admin form checks                                                                       | Targeted `use-scroll-to-feedback` Vitest; manual member-dialog validation and success checks                                                                                                                                                                                                                                                                                                                                                                                                 |
| UI/UX journey              | Admin/finance operator               | Open the floating Help widget on any surface and read route-specific guidance — curated question chips in the Ask view and the full Page guide (common actions, key fields, setup hubs, Membership Type Xero-rule modes, and complex sections)                                                                                                                                                                                                     | Low      | Registry unit tests plus help-widget component and corpus tests                                                     | Targeted contextual-help registry tests, `src/components/help-widget/__tests__/help-widget.test.tsx` (chip Q/A, Page guide fields/sections, deep-link href, aria-pressed toggles), and `src/lib/help/__tests__/help-corpus.test.ts` plus membership-types page copy tests; manual `/admin/members`, `/admin/membership-types`, `/admin/xero/setup`, and `/finance` smoke                                                                                                                                                                                                                                                                                                          |
| UI/UX journey              | Admin/visitor                        | Edit shared site footer content from Admin > Site Content and render the sanitised, token-resolved columns on public website pages                                                                                                                                                                                                                                                               | Medium   | API, migration/backfill, component, and manual public footer checks                                                 | Targeted site-content API/backfill/footer tests plus `/admin/site-content` and public footer smoke                                                                                                                                                                                                                                                                                                                                                                                           |
| UI/UX journey              | Admin/visitor                        | Publish site-wide notice banners with priority colour, inclusive NZ date-only display windows, per-browser dismissal, and edit-triggered re-display                                                                                                                                                                                                                                              | Medium   | API, service, component, and manual public/member header checks                                                     | Targeted site-banner API/service/component tests plus `/admin/site-banners` and public/member header smoke                                                                                                                                                                                                                                                                                                                                                                                   |
| UI/UX journey              | Visitor/member/admin                 | Happy path and empty/failure/pending states                                                                                                                                                                                                                                                                                                                                                      | Medium   | Component tests plus manual staging checks                                                                          | `npm run lint`, targeted component tests, staging route checklist                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Accessibility              | Visitor/member/admin/lodge           | Keyboard, headings, labels, contrast, focus, touch targets                                                                                                                                                                                                                                                                                                                                       | Medium   | Manual staging and Lighthouse on non-production                                                                     | See `docs/STAGING_ACCESSIBILITY.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Booking capacity + membership lifecycle | Member/delegate/admin/cron | Member-guest consent ("+ Add Member Guest", #2305 / MG2 #2307): a cross-family member add resolves only with the memberGuests module on, and persists PENDING (ask-first, the shipped default), CONFIRMED-never-asked (notify-only), or CONFIRMED-against-the-admin (on-behalf, booking copy, pipeline). The target or an accepted family-adult delegate approves or declines through one endpoint whose every failure is a byte-identical 403. A pending guest HOLDS a bed and a person-night but is excluded from all nine operational surfaces including the two arrival emails (D-12). A nightly sweep expires lapsed requests, electing account credit to the booking owner so a paid booking releases on time (D-15); the four cases it cannot resolve (last guest, quote-priced, status forbids changes, check-in started) survive as blocked rows on an admin exception list rather than silently holding capacity. Owner decision D-14 keeps the ordinary self-removal blockers in force for a never-consented guest, so a decline is refusable — that is the behaviour, not a gap | Critical | Vitest: state-machine + concurrency, the consentAuthority security boundary, the delegate accept/reject matrix, the D-14 trap matrix, the D-12 exclusion sites plus the capacity freeze list, the sweep's idempotency and its deterministic LAST_GUEST ordering, all four email templates against the registry and suppression contracts, and the widening contract suite that inverted MG1's dark guarantee. Playwright `member-guest-consent.spec.ts` drives the shipped surfaces end to end: a cross-family add persists PENDING and the target approves on the booking page's consent card (badge flips to Consented); the target declines on an unpaid booking and the booker's guest list no longer names them; and the delegate route's privacy edge — a signed-in non-delegate, including the booker, gets one neutral page from `/bookings/consent/<guestId>` with no booking facts on it, indistinguishable from a fabricated id | Targeted `member-guest-consent`, `member-guest-widening`, `member-guest-consent-service`, `member-guest-delegate`, `cron-member-guest-consent-expiry`, `booking-guest-removal`, `member-guest-settings`, `email-message-registry`, `email-message-token-contract`, `booking-no-emails-ui-contract`, `cron-recording-health-contract`, `admin-route-area-matrix`, and the nine D-12 surface suites; plus `npm test` in full, because the registry and matrix suites are exactly the ones a targeted run misses |
| Booking privacy + enumeration | Member/admin | Member-guest FIND and the #2388 mitigations (MG3 #2308): a booker finds another member by exact email (always, module-on) or by name where a club deliberately turned open search on, from ONE box that takes either. Both routes 404 when their gate is off — never 403 — and the name route starts and stops 404ing the moment the setting changes, with no deploy. Neither route evaluates eligibility at all, so it cannot become an eligibility oracle; the envelope is always 200 and identical for found, not-found and inactive. Rows carry full name and age tier only (D-19); minors are excluded from the type-ahead by default (D-20) but stay resolvable by household email (D-9); the cap is ten, matching is prefix-only, and overflow is a boolean rather than a count. Every query in both modes is rate-limited per acting member as well as per IP and audited, including empty, under-minimum and blocked ones. On the ADD paths, #2388's three decided mitigations across all SIX member-facing add paths: per-acting-member throttling counted whenever a cross-family member is INVOLVED (on `modify-quote` that includes a preview whose booking merely carries one and whose `addGuests` is empty — the cheapest surface in the product, and free until the whole-party charge closed it; the route's two charge points share one ledger so an attempt still costs one unit), a response floor on the collapsed refusal that is honestly a MINIMUM rather than an equaliser, and the three refusals that used to escape D-8's collapse entirely ("inactive or not found", age-exempt, and the membership-type policy refusal, which named the blocked member and their membership category outright) now collapsing too. Repeated same-target refusals raise an admin-visible audit row — once per actor/target per 24-hour window, on the crossing rather than on every refusal past it — and are NEVER blocked — the owner's explicit sub-decision, since an honest retry across dates is indistinguishable from probing without a person looking | High | Vitest: the pure find model (prefix parsing, caps, row shape, copy), the two routes at route level (both 404 gates, the uniform envelope, the minors filter, the cap, the audits, the rate limits), the eight-state x three-audience badge table with a totality check against the schema's own sub-state list, the probe guard (throttle scoping, timing floor, never-block), and the widened D-8 collapse. Eighteen mutation probes were run against the guards named above and all eighteen were killed — a claim scoped deliberately, because a later review found the WIZARD add path outside that set entirely and two mutations surviving in it (deleting the credit-election reset, and deleting both add guards). Both are now covered by `use-booking-wizard-member-guest.test.tsx`, which asserts the member-guest add against the FAMILY add's own behaviour rather than a copied list, and by `member-guest-find-panel.test.tsx`, the find panel's first component test. The person-night guard's cross-family marking is additionally pinned by a source-scanning contract: every caller of the guard under `src/` must be classified as either marking the whole proposed party or exempt with a written reason, so a new caller cannot silently re-open the read-out, and every guard call in a file must have its OWN preceding marking call rather than only the file's first. The privacy RE-REVIEW's four findings carry their own suites: `member-guest-membership-type-refusal.test.ts` checks the SERIALISED membership-type refusal for the name, the email fallback, the member id and the membership category; `member-guest-boundary-gate.test.ts` pins the owner's gate on the family-boundary recomputation including its consent-row arm and its fail-closed abstention; `edit-booking-panel-member-guest-refusal.test.tsx` drives the debounced date-change sequence that used to be told an add had failed. Six further mutation probes were run and all six killed (deleting the whole-party throttle charge, letting the collapsed message and then the body name the member again, making the gate always skip, reverting the once-per-crossing warning dedup, and disabling the panel's re-wording). Playwright `member-guest-find.spec.ts` drives one journey per mode plus a module-off assertion; its browser assertions run LAST in the serial file, so a navigation failure in one cannot skip the privacy assertions | Targeted `member-guest-find`, `member-guest-probe-guard`, `member-guest-consent-card`, `member-guest-cross-family-refusals`, `member-guest-membership-type-refusal`, `member-guest-boundary-gate`, `membership-type-policy`, `edit-booking-panel-member-guest-refusal`, `review-findings-contracts`, `rate-limit`, and `src/app/api/members/guest-candidates`; plus `npm test` in full, because the route-area and registry suites are the ones a targeted run misses |
| Booking guests on the edit and admin paths | Member/admin/officer | Member-guest edit path, admin parity and the booking-request pipeline ("+ Add Member Guest", #2305 / MG4 #2309): a member can add a member guest while EDITING a booking, not only while creating one, through the same modification path that already resolved the family boundary — so consent state, pricing reset, capacity, the person-night guard and D-8's neutral refusal are the create path's, not a second set. Admins get the same section on the same page (there is no separate admin booking page) with admin copy: the add is immediate, CONFIRMED against the acting officer, and ALWAYS notifies (MG4-D-a). The officer's member picker is a separate booking-scoped route whose NAME mode is gated on membership:view rather than bookings:edit, so #1376's directory-less Booking Officer falls back to exact-email resolve; every officer lookup is audited through the same two writers a member's is, and the officer's search is not bound by the club's member-facing minors switch (D-20). The booking-request pipeline's THREE write points — the capacity hold's create, the approval-time guest swap, and the approval with no hold behind it — all stamp ADMIN_ASSIGNED and notify (MG4-D-b); the swap preserves each row's id for bed survival, so a substitution is detected by memberId transition and BOTH parties are told, and a reused row's stale consent is cleared. A member guest who comes OFF a booking is told once, by the removal route or the batch edit and by nothing else (a decline and a lapse each already have their own message, and a self-removal tells nobody). Owner decisions D-13 and D-14 are tested as ABSENCES: an edit never touches an already-consented guest's five columns and no change-notification template exists, and nothing anywhere offers a self-removal the shared predicate would refuse | High | Vitest: the D-13 absence over `applyGuestChanges` (a date change that lengthens a stay leaves all five consent columns off the update), the no-change-template and no-re-consent-copy sweeps, the D-14 trap matrix over LAST_GUEST / QUOTE_PRICED / STAY_NOT_FUTURE asserting the note matches the predicate and names a real remedy, the copy's four-source-state matrix (PENDING / CONFIRMED / DECLINED / EXPIRED all re-stamped ADMIN_ASSIGNED against the copying admin, family and non-member rows untouched), the pipeline hold and the in-place substitution, the officer picker's gate and audits, and the section component's copy and route selection. Six mutation probes were run and all six killed: dropping the pipeline's consent stamp, blinding the substitution detection, letting the copy inherit its source's consent, resetting consent on a date change, dropping the quote-priced fact from the removal note, and removing the membership:view gate from name search. Playwright: `member-guest-edit-path.spec.ts` in the multi-lodge matrix drives a member adding a member guest from the edit panel and asserts the target's mail in Mailpit; `member-guest-admin-add.spec.ts` drives the officer's add and its always-notify | Targeted `member-guest-edit-path`, `member-guest-add-notifications`, `admin-booking-copy`, `booking-request-quotes`, `reassign-held-booking-guests`, `booking-request`, `school-booking-request`, `member-guest-widening`, `edit-member-guest-section`, and `src/app/api/admin/bookings/[id]/member-guest-candidates`; plus `npm test` in full, because the consent-column census, the route-area snapshot and the email registry suites are the ones a targeted run misses |
| Operations/deploy          | Maintainer/operator                  | Migration safety, blue/green compatibility, CI gates                                                                                                                                                                                                                                                                                                                                             | High     | Script tests and dry-run validation                                                                                 | `git diff --check`, migration safety scripts on non-production data                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Lodge operations           | Member/committee/admin               | Events calendar (#calendar, #2241): the whole surface is module-gated on `eventsCalendar` (default ON — module off 404s `/calendar`, `/admin/calendar` and `/api/calendar/*`, drops the admin sidebar/command-palette entry, and skips both the dashboard Events card and its query; events are retained and reappear when it is switched back on), and organisation ("ORG") accounts are excluded from it outright by the single `canViewCalendarEvents` predicate that guards reading AND writing, so an ORG account holding a committee assignment still cannot create. Within that, every logged-in non-organisation member views the read-only month at `/calendar`; a committee member (active CommitteeAssignment under an active CommitteeRole) may CREATE events and join MiroTalk meetings but NOT edit or delete; only a lodge-edit admin edits or deletes (on `/admin/calendar`, itself lodge-area gated so a committee-only member is redirected out). Ordinary members see event details but no "Join meeting" link; the "+N more" day-overflow opens a real day-detail dialog for BOTH members and managers so the 4th event onward stays reachable; meeting join URLs are built server-side from MIROTALK_URL, defaulting to https://meet.<domain> when unset (loopback app host → localhost dev). The write gate is server-authoritative — create is committee-or-admin, edit/delete admin-only — so a committee member can never wipe an admin's series | High     | Access-gate, write-route, dialog-gating, day-overflow, recurrence, and meeting-URL Vitest suites                    | Targeted `calendar-access` (create gate: admin / committee / plain; edit gate: admin-only, committee denied without querying the committee table), `calendar-event-write-gate` (committee PATCH/DELETE → 403 with the service untouched, admin DELETE → 200), `event-dialog-join-gating` (Join shown only when canManage), `day-overflow` ("+N more" opens the day detail for member and manager, lists the 4th/5th, Add-event only for managers), `calendar-events` (MiroTalk base-URL derivation: loopback fallback, meet.<domain> from NEXTAUTH_URL, www strip), `calendar-service` (recurrence propagate-vs-regenerate per scope), `calendar-recurrence`, and `calendar-client` Vitest suites, plus `events-calendar-module-gate` (both page guards notFound on module-off and on an ORG viewer, the write gates short-circuiting on the view gate, and the dashboard card + its query being skipped), `calendar-list-view-gate` (GET /api/calendar/events → 404 for an organisation) and the `feature-routes` prefix/proxy-matcher assertions for `/calendar`, `/api/calendar` and `/admin/calendar`; no dedicated browser E2E (gated at route/service/component level) |
| Finance reporting | Admin with Finance view | Base Reports (#2368): choose an inclusive NZ date-only range and retain Lodge/Deleted scope; bookings, distinct guests, trends, current-status breakdown, and Booked Revenue follow overlapping stay nights from the explicit PENDING/PAYMENT_PENDING/CONFIRMED/PAID/AWAITING_REVIEW/COMPLETED cohort. Full `finalPriceCents` is allocated before range slicing and cards/chart/PDF preserve exact cents; Net Collected Cash remains payment-derived and Outstanding Additions remains visible. The #2408 additional-ledger-gap guard keeps cash arithmetic unchanged, logs affected booking IDs, and exposes only an aggregate warning in the API/page/CSV/PDF. Occupancy stays PAID/COMPLETED-only and excludes custodians. **Next Month** still selects the complete next calendar month without changing Lodge/Deleted. | Medium | Pure/property, exact-drift route, component/render/export Vitest plus Playwright and named screenshot | Targeted `admin-reports`, `admin-reports-route`, `finance-booking-metrics`, contextual-help, date-range controls/page/chart suites; Playwright `e2e/admin-reports.spec.ts` uses the seeded created-now/stayed-40-days-ago CONFIRMED booking to prove stay-night selection, exact `$135.00` booked revenue/cash labels, and the card-scoped occupancy exception; named screenshot `admin-reports` waits for Net Collected Cash and writes `docs/images/admin/admin-reports.png` |
| Finance reporting | Admin with Finance view | Choose **Next Month** from Reports Quick Range while retaining Lodge and Deleted scope; the range covers the complete next calendar month in NZ date-only semantics | Low | Focused Vitest component/date logic; manual screenshot | `npx vitest run src/lib/__tests__/date-range-presets.test.ts src/components/admin/__tests__/date-range-controls.test.tsx "src/app/(admin)/admin/reports/__tests__/page.test.tsx"`; screenshot remains `docs/images/admin/admin-reports.png` and is coordinated with #2358 |

| UI/UX journey              | Admin/finance operator               | Reset an audited dataset after search-only, filter-only, explicit-sort-only, page-only, or combined changes; Reset is always rendered, disabled only at the true dataset default, and restores search/filters/sort/page while preserving lodge, season, tab, cohort, focused record, Finance view, sibling Xero panel state, and unknown forward-compatible URL keys. Payments restores the rolling NZ three-month Updated range through today; Reports restores its month-boundary range through current month-end and keeps lodge; Finance Dashboard restores Last Month / Previous Period / Next Month and empty expense filters while keeping view and lodge. URL-backed surfaces replace rather than push browser history; Reports stays local-state. | Medium   | Shared component and state-helper unit tests; source inventory contract; targeted page/component tests; manual browser smoke after screenshot-owner overlap is resolved | `dataset-reset-button`, `admin-dataset-reset-state`, `dataset-reset-contract`, `finance-dashboard-client`, Members hook and booking-filter tests, booking-request resolution and Xero page tests; mutation-check the inventory count and a namespaced URL key. Manual light/dark and keyboard smoke on Members, Bookings, Payments, Subscriptions, Reports, and Finance Dashboard; verify one Back press skips intermediate filter state. Screenshot refresh/capture remains an orchestrator follow-up. |

## Baseline Safe Checks

```bash
git diff --check
npm run lint
DATABASE_URL=postgresql://user:pass@localhost:5432/tacbookings npx prisma validate
npx tsc --noEmit
npm test
```

Do not run browser automation, load tests, DAST, live provider calls, or
production-like endpoint scanning against production without a written test
window.

## Custodian occupancy (#2286): unit-only, deliberately

The two custodian rows above are validated by unit and integration suites, not
by Playwright, and that is a decision rather than a gap.

The behaviour that matters is **arithmetic and refusal**: which bed-nights leave
the pools, which write paths refuse, and which names the display serialiser
withholds. Every one of those is a pure function of a `HutLeaderAssignment` row
plus a date, and each is asserted directly — including the mutation-sensitive
cases (a reconcile leaving a held bed empty, a hold created *between* the plan
and the write, a range refusing only the held nights). A browser cannot observe
any of it more sharply than the unit assertions do.

The minor-suppression contract in particular is asserted at the serialiser,
which is the only place it can be enforced: the payload is what leaves the
building, so `custodian-display.test.ts` asserts the built payload contains no
minor's name at `FULL_NAME` granularity. Driving the same case through
`e2e/lobby-display.spec.ts` would need a bed-holding assignment for a
minor-age member seeded into the staging stack, and would then assert on
rendered text that the serialiser had already been proven never to produce —
weaker evidence at a much higher cost. If a future change moves any of that
decision into the template, this row must be revisited.
