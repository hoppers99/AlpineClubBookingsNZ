# Phase-2 refutation fixtures

These inert fixtures are consumed by `../bin/self-test.mjs`. They prove the
harness rejects a cache MISS where a current HIT is required and rejects a body
whose checksum differs from the correctness manifest. They are not timing data.
